<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Black launch window so there is no white flash before Compose draws. -->
    <style name="Theme.RespawnSuite" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/terminal_black</item>
        <item name="android:colorBackground">@color/terminal_black</item>
    </style>
</resources>
