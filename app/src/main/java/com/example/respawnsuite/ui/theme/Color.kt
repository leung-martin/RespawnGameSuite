package com.example.respawnsuite.ui.theme

import androidx.compose.ui.graphics.Color

/** Phosphor-terminal palette: black ground, green text, nothing else. */
val TerminalBlack = Color(0xFF000000)
val TerminalGreen = Color(0xFF00FF41)
val TerminalGreenDim = Color(0xFF00A82B)
val TerminalGreenFaint = Color(0xFF0A5A19)
val TerminalAmber = Color(0xFFFFB000)
val TerminalRed = Color(0xFFFF3B30)

/**
 * The one non-phosphor colour in the suite, for controls that must be reachable
 * without drawing the eye — the abort bar sitting under a live round.
 */
val TerminalGray = Color(0xFF9AA0A6)
val TerminalGrayDim = Color(0xFF3C4043)
val TerminalGrayFaint = Color(0xFF26282B)
